# 🚀 减重打卡小程序 · 上线手册

> 目标：从"代码文件夹"到"微信里能打开用"。全程约 30~60 分钟，大部分是等审核。
> 有任何一步卡住，把**截图/报错文字**发给 AI，会帮你解决。

---

## 第 0 步：确认电脑上有什么

| 物品 | 要求 |
|---|---|
| 微信开发者工具 | 未安装 → https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html 下载稳定版 |
| 小程序 AppID | 未注册 → 走第 1 步 |
| 项目文件夹 | `F:\微信小程序\jianfei-miniprogram` ✅ 已就绪 |

---

## 第 1 步：注册小程序（免费，约 10 分钟）

1. 打开 https://mp.weixin.qq.com
2. 右上角「立即注册」→ 选择「**小程序**」
3. 用一个**没注册过公众号/小程序的邮箱**注册 → 收邮件点激活链接
4. 主体类型选「**个人**」→ 填身份证、手机号、微信扫码验证
5. 完成后进入小程序后台 → 左侧「设置 → 开发管理 → 开发设置」→ 复制 **AppID**（形如 wx1234567890abcdef）

> ⚠️ 个人主体：免费、无需营业执照。可以发布小程序，只是不能用微信支付/手机号登录等企业功能（本小程序已规避这些）。

## 第 2 步：导入项目并首次预览（约 10 分钟）

1. 打开微信开发者工具 → 微信扫码登录
2. 点「+」→ 导入项目：
   - 目录：`F:\微信小程序\jianfei-miniprogram`
   - AppID：填第 1 步拿到的（**不要选测试号**，测试号无法真机预览和用云开发）
3. 点「确定」→ 左侧模拟器应出现**登录页**
4. 顶部工具栏点「**预览**」→ 用手机微信扫码 → 手机上打开体验
5. 手机上：勾选协议 → 点「微信一键登录」→ 此时未配置云开发，会进入**本地体验模式**，所有打卡功能都可先用

> ✅ 到这一步，小程序已经"能用"了（只有你自己能打开）。
> 想省事，可以跳过第 3、4 步直接去第 5 步（但云同步/家人/提醒/正式登录这些功能会不可用）。

## 第 3 步：云开发（环境已配好，只差部署云函数，约 5 分钟）

> ✅ 云开发环境 ID（`wxc641774f15ae0cd9`）已写入 `utils/config.js`，无需再创建。

1. 左侧文件树展开 `cloudfunctions`，对 **5 个文件夹（login、family、reminder、vision、ai）分别右键 →「上传并部署：云端安装依赖」**
2. `reminder` 再右键 →「**上传触发器**」（定时发送提醒用）
3. 回到模拟器 → 编译 → 手机上重新预览 → 登录页点「微信一键登录」→ 走**正式登录**（获取 openid 建账号）

> 数据库集合 `sync`/`users`/`families`/`reminders` 会在第一次使用时自动创建，权限保持默认即可。
> 若右键没有「上传并部署」选项：说明项目按"本地开发"方式打开，需重新导入一次并在导入时把后端服务选为「微信云开发」。

## 第 4 步（可选）：AI 功能 + 订阅消息（各约 3 分钟）

### 4a. AI 拍照识别 / 点评 / 饮食点评（已配好 Key，只差部署）
1. ~~注册智谱 AI 开放平台 https://open.bigmodel.cn~~ ✅ API Key 已配置在 `cloudfunctions/vision/key.local.js` 和 `cloudfunctions/ai/key.local.js`（此文件不进 GitHub，部署时随文件夹上传云端）
2. 部署 `vision` 和 `ai` 两个云函数（第 3 步已做则跳过）
3. 热量页「📸 拍食物」拍照识别；识别后「🤖 AI 点评这顿饭」；首页「🤖 AI 点评今日饮食」

### 4b. 订阅消息提醒（喝水/称重/周报）
1. mp 后台「功能 → 订阅消息」→「公共模板库」搜索「**提醒**」→ 选**三个**提醒类模板（喝水、称重、周报各一个）→ 添加到我的模板 → 复制**模板 ID**
2. 填入 `utils/config.js`：
   - `remind.water.id` = 喝水提醒模板 ID
   - `remind.weigh.id` = 称重提醒模板 ID
   - `remind.weekly.id` = 周报推送模板 ID（周报内容由云函数自动生成，模板需含 thing 字段）
   - 模板字段名和 `thing1`/`time2` 不一致时，照着模板的字段改 `data` 里的键名
3. `cloudfunctions/reminder/index.js` 里的 `MINI_STATE`：开发调试改 `'developer'`，体验版改 `'trial'`，正式发布用 `'formal'`

## 第 5 步：让家人一起用（体验版，约 10 分钟）

1. 开发者工具点「**上传**」→ 版本号填 `2.0.0`，备注随意
2. mp 后台「管理 → 版本管理」→ 开发版本列表找到刚上传的 → 设为「**体验版**」
3. mp 后台「管理 → 成员管理 → 体验成员」→ 添加家人的微信号（扫家人的码邀请）
4. 家人打开体验版二维码即可使用（先各自在「我的」页创建/加入家庭组）

## 第 6 步：正式发布（1~7 天等审核）

> 📋 本步骤所有要填的文案（隐私指引逐字段内容、类目说明、审核自查清单、版本备注）都已写好在同目录的 **《上线材料包.md》** 里，照抄即可。

1. **配置用户隐私保护指引**（必做，漏了必被驳回）：
   - mp 后台「设置 → 服务内容声明 → 用户隐私保护指引」→ 按《上线材料包.md》第一节逐条填写
2. mp 后台「管理 → 版本管理」→ 开发版本 → 「提交审核」：
   - 类目：按《上线材料包.md》第三节建议选择
   - 版本备注：复制《上线材料包.md》第五节文案
3. 审核通过后点「**发布**」→ 所有人都能搜索/分享使用 🎉

## 📁 附带文件

- `F:\微信小程序\减肥\README.md` — 完整开发文档（项目目录即 `F:\微信小程序\减肥`）
- `F:\微信小程序\减肥\上线手册.md` — 本手册
- `F:\微信小程序\减肥\上线材料包.md` — 审核所需文案（照抄即用）
- `F:\微信小程序\界面预览\` — 12 张 UI 预览图
- `F:\微信小程序\tests\` — 测试脚本（4 套 218 项断言，运行 `node run_tests.js` 等，需设环境变量 `MP_SRC=F:\微信小程序\减肥`）
- GitHub：https://github.com/yanm6172-glitch/- （项目在 `Yanmeng/jianfei-miniprogram/`，密钥文件不入库）

---

## 🔧 常见报错速查

| 现象 | 原因与解决 |
|---|---|
| 预览二维码提示"未找到 AppID" | `project.config.json` 里还是 `touristappid`，改成你的真实 AppID |
| 登录提示"登录失败，请检查云函数" | `login` 云函数没部署，或 `config.js` 的 cloudEnv 没填/填错 |
| 提示 `cloud init error` | 环境 ID 填错，或没在工具里开通云开发 |
| 扫码查热量一直失败 | 开发者工具「详情 → 本地设置」勾选**不校验合法域名**；正式发布前在后台配 `request` 域名（见 README） |
| 订阅消息发不出去 | 模板 ID 未填/填错；字段名与模板不符；`MINI_STATE` 与当前环境不匹配 |
| 深色模式没反应 | 需要基础库 2.11.0+；开发者工具「详情 → 本地设置」里选最新调试基础库 |
| 修改了 config.js 不生效 | 改完要点工具上的「编译」重新构建 |
| 模拟器正常、真机异常 | 大概率是域名/云函数权限问题，把真机报错截图发 AI |
